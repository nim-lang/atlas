# Package

version = "0.4.0"
author = "Araq"
description = "Atlas is a simple package cloner tool. It manages an isolated workspace."
license = "MIT"
srcDir = "src"
skipDirs = @["doc"]
bin = @["atlas"]

# Dependencies

requires "nim >= 1.6.0"
